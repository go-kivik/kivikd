dist: trusty
sudo: required
services:
    - docker
addons:
  apt:
    sources:
      - sourceline: 'ppa:masterminds/glide'
    packages:
      - glide
cache:
    directories:
        - node_modules
        - $HOME/gopath/pkg
        - /var/lib/docker

language: go

os:
    - linux
    - osx

go:
    - 1.7.x
    - 1.8.x
    - 1.9.x
    - master
env:
    global:
        - HOMEBREW_NO_AUTO_UPDATE=1
    matrix:
        - MODE=standard

matrix:
    allow_failures:
        - go: master
    include:
        - go: 1.9.x
          os: osx
        - go: 1.9.x
          env: MODE=linter
        - go: 1.9.x
          env: MODE=coverage

install:
    - ./travis/install.sh $MODE

script:
    - ./travis/test.sh $MODE
